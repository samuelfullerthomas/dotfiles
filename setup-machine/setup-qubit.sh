#!/bin/sh
fusion machine            # install a base set of software (Mac only)
fusion restore            # import DBs form backups
fusion update             # clone and bootstrap all services
fusion boot experiences   # start running the app
